APPROVE

All 4 edits are well-scoped, correct root causes confirmed by grep, no new dependencies, permission model preserved via prop-threading + canManageBudget gate. Non-goals are sensible. Ship it.
